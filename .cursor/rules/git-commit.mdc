---
description: 助理只要有改檔，回覆結尾必附 commit 指令（不代為執行）。
globs: "*"
alwaysApply: true
---

# Git commit：改檔必附指令

## 死線（優先於其他結尾習慣）

**只要本次回覆對應的任務有改動 repo 內任一檔案**（含 `.html`、`.php`、`.css`、`.js`、`.md`、`.mdc`、`CURSOR-HANDOFF.md` 等），該則回覆**結尾必須**附上 commit 指令區塊——**主動、強制、不可遺漏、不可等使用者開口**。

- 漏附 = 未遵守本 repo 規範。
- **純聊天、觀念問答、本則完全沒改任何檔** → 不必附。

## 輸出格式（一字不差）

回覆最後一段放：

```bash
git add .
git commit -m "type: 重點"
```

- `type` 與 `重點` 依下方規則填寫；`-m` 內容須反映**本次實際改動的檔案與目的**。

## type（擇一）

採用常見的 [Conventional Commits](https://www.conventionalcommits.org/zh-hant/) 前綴，方便日後用 `git log` 篩選。

- `fix:` — 修正錯誤、壞連結、顯示異常
- `feat:` — 新功能、新頁面、新單元、新工具行為
- `docs:` — **學習／說明內容**：`notes/` 辦案筆記、`README`、`CURSOR-HANDOFF.md`、規則 `.mdc` 等（含「標點複習完畢移入已鑑識」）
- `style:` — **外觀與版面**：`Conan.css`、配色、間距、頁尾文案精簡等，不改變頁面「教什麼」

**怎麼選（一句話）**：改的是「學到什麼」→ `docs:`；改的是「長什麼樣」→ `style:`。

## 重點（`type: ` 之後）

- **不超過 15 字**（中文一字算一字）
- **只寫 repo 內實際改動**；勿寫聊天教學、原因補充、對話才提到的說明
- 範例：`docs: SQL 逗號移入已鑑識`、`style: 統一標題配色`

## 勿擅自執行

- **只提供指令文字**；除非使用者當次明確要求，否則**不代為**執行 `git add` / `git commit` / `git push`。
