---
description: conan-school 協作慣例（時區、還原、辦案筆記三處同步）
globs: "*"
alwaysApply: true
---

# 米花町偵探學園（conan-school）協作慣例

> 與 [WebDev-Notes](https://github.com/jekkinoopy/WebDev-Notes) 共用核心規範；母 repo 可攜包：`docs/portable-collab-rules/`。

## 時區與「昨天／今天」

- 以**使用者所在地（預設台灣 UTC+8）**理解「昨天、今天、剛剛」。
- **不可**只憑 `git log` 的 commit 日期就認定「昨天」並 `reset`。
- 使用者說「還原到昨天／弄壞前」時：先 `git log` / `reflog` 列出候選 commit，**確認要回到哪一個 SHA** 再動手；未 push 時以**本地 HEAD／reflog** 為準。

## Git 還原

- 禁止未確認就 `git reset --hard`。
- 還原後若本地落後遠端，**提醒勿盲目 `git pull`**（會拉回不要的提交）；若要改遠端須使用者同意後再 `push --force`。

## 未要求勿更動

- **只動使用者點名的檔**；勿順手改未點名檔案。
- 使用者**已刪除**的區塊／樣式，未經當句需求明寫**不得擅自加回**。

## 辦案筆記 `notes/`

改動由**助理**完成時，須自責維持 **三處一致**（勿假設使用者手動刪檔）：

| 必須同步 | 路徑 |
|----------|------|
| 筆記檔 | `notes/NN-標題.html` |
| 目錄條目 | `notes/index.html` 的 `<ol class="conan-notes-index-list">` |
| 上下則 | `conan-notes-nav.js` 的 `CHAIN` |

- **新增**一則：三處都加；內頁要有 `#conan-notes-nav-root` + `data-note-id`（與 `CHAIN[].id` 一致）。
- **刪除**一則：三處都刪。
- **改 `notes/index.html`、頁尾、peer-nav、更名**後：用 `notes/*.html` 對照目錄與 `CHAIN`；**禁止**用舊版 `index.html` 覆寫而漏條目。

## 筆記內容

- 讀者正文只寫**知識與題目**；協作說明、聊天口吻**勿寫進 HTML**。
- 頁底 `conan-notes-nav` 已負責上下則；**不要**在正文加「下一步：請進入 xxx」。

## 對外連結

- PHP 課程筆記連 [WebDev-Notes Pages](https://jekkinoopy.github.io/WebDev-Notes/courses/01-database/php/index.html)，勿改回不存在於本 repo 的相對路徑 `../../../01-database/...`。

## 決策

- 有 repo 慣例就照做；**使用者明確要求**的優先於助理自判「不必」。
